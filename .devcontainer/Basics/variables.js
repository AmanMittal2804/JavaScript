const accountID= "1765332"
// Const is the value which will remain same all over the codebase

let emailID= "mittalaman2804@gmail.com"
// Only the value will be defined inside the block scope 

var accountPassword = "Arup4567"
// If changes in the value take place it will change all values around the codebase

console.table([accountID,accountPassword,emailID])